<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base dark theme -->
    <style name="Base.Theme.QuickMark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/green</item>
        <!-- Make status bar icons light (white) -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Primary colors -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryContainer">@color/green</item>
    </style>

    <!-- App dark theme -->
    <style name="Theme.QuickMark" parent="Base.Theme.QuickMark"/>
</resources>